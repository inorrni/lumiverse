---
title: Lumiverse 이슈 · 개선 트래킹
updated: 2026-06-21
용도: 알려진 문제·개선 항목을 누적 기록. 최신이 위로.
related: "[[백엔드연동_점검_260616]] · [[개발일지_260616]] · [[개발일지_260617]]"
---

# 이슈 · 개선 트래킹

> [!note] 작성 규칙
> 한 행 = 한 이슈. 상태: 🔴 미착수 / 🟡 진행중(담당 배정) / 🟢 해결. 해결 시 해결 커밋/PR을 비고에 남긴다.

| # | 상태 | 이슈 | 영향 | 담당 | 비고 |
|---|---|---|---|---|---|
| O10 | ⚪ 무효 | 어떤 환경에서 화면 이미지가 전부 깨짐(못 불러옴) | 표시/배포 | — | 0621 — **코드 무관**: localhost 연결 끊김이 원인이었음(자산 경로 정상). 종료. cf. [[트러블슈팅_대시보드_260621]] D-3 |
| O9 | 🟢 해결 | 행성상세 목록에서 행성 진입 후 뒤로 → 재클릭 무반응 | 핵심 동선 | — | 0621 — 행성·은하 상세가 같은 라우트 `/app/planet/:id` 인스턴스 재사용(in-place 재조정). `PlanetDetailPage`를 `key={id}`로 강제 리마운트. **실기기 정상 확인.** cf. [[트러블슈팅_대시보드_260621]] D-1 |
| O8 | 🟢 해결 | 메인 Today 카드가 읽기 전용(체크/회고 불가) + 행이 과대 | 핵심 루프/UX | — | 0621 — `TodayRow`→`CheckRow`+`ReviewField`(별 토글·회고), `ReviewField` 공용 분리. `ink` 행 높이 절반·회고칸 제목 밑. Today 디데이 제거. cf. [[개발일지_260621]] |
| O7 | 🟢 해결 | 설정에서 닉네임 수정 불가 | 설정/온보딩 | — | 0621 — 설정 "프로필" 섹션 인라인 편집(기존 `updateNickname` 재사용) |
| O6 | 🟢 해결 | 로그인 후 정보 있는 유저도 온보딩(`/mode`)으로 빠짐(K3와 동일 레이스) | 진입 라우팅 | — | 0621 — 닉네임 있으면 `goals` 로드 무관 `/app` 직행(`goals.length` 분기 제거). K3 해소 |
| O5 | 🟢 해결 | 별자리 생성 시 개수·시점(더 키우기/지금) 선택 불가 | 보상 통제 | — | 0621 — `ConstellationModal` 확인 단계 + 개수 스테퍼(14~모은 별), 선택 개수로 생성 |
| O4 | 🟢 해결 | 별자리 안내가 짧은 목표(총 별<14)에 도달 불가능한 약속 표시 | 카피/동기 | — | 0621 — 총 별≥14면 "N개 더", 미만이면 별자리 언급 없는 긍정 문구. 완성 시 버튼 제거·가운데 정렬, 미생성도 중앙 정렬 |
| O3 | 🟢 해결 | Daily Stars 분모에 기간종료 목표 행성까지 포함 → 100% 도달 불가 | 표시 정합 | — | 0621 — 분모를 오늘 별 있는 행성만(`todayStarId`)으로. cf. [[트러블슈팅_대시보드_260621]] D-2 |
| O2 | 🟢 해결 | 디데이 지난 목표 미구분('∞') + 대시보드 다목표 비대 | 상태/동선 | — | 0621 — `deriveGoal` `lifecycle`(active/expired/completed) 파생(DB 0), 대시보드 "오늘 별 있는 목표만", 우주탭 상태 필터(+중간점검 단계) |
| O1 | 🟢 해결 | 목표 카드에 마감 날짜 미표시 | 표시/정보 | — | 0621 — `formatDday`/`ddayDelta` 추가, 카드에 `~YYYY.MM.DD · D-N`+모드 태그, `새 목표`/`전체 보기`는 카드 바깥으로 |
| N6 | 🟢 해결 | 투두 회고 입력칸이 행 아래 가로선과 함께 분리돼 보임 | 가독성/입력 | — | 0620 — 회고칸을 별 제목 바로 밑·가로선 없이 배치(`.item` 래퍼가 구분선, `CheckRow`는 `last`로 자체 보더 제거), 중복 `.item`/`.reviewInput` CSS 정리. 공유 `CheckRow` 불변(행성상세 무영향). 커밋 `64a5aae`. cf. [[개발일지_260620]] |
| N5 | 🟢 해결 | 투두 하단 필터가 "완료된 별만"이라 남은 할 일 추리기에 부적합 | 핵심 루프/UX | — | 0620 — "완료 안 된 별만 보기"로 전환(`doneOnly`→`undoneOnly`, `!r.checked`, 빈 문구·라벨 정리). 커밋 `64a5aae`. cf. [[개발일지_260620]] |
| N4 | 🟢 해결 | 대시보드 은하 → 투두 진입 시 모든 드롭다운이 접혀 그 은하를 다시 찾아야 함 | 동선/UX | — | 0620 — Today "모두 보기"가 `state.goalId` 전달, `TodayPage`가 그 은하를 펼친 채 시작(`useLocation`+`expanded` lazy init). 탭 직접 진입은 기존대로 접힘. 커밋 `64a5aae`. cf. [[개발일지_260620]] |
| N3 | 🟢 해결 | 블랙홀에서 행성 복구해도 보완으로 추가한 새 행성이 Today에 안 보임(N1과 동일 원인) | 보관함/표시 | — | 0620 — N1 수정(Today 카드 slice 제거)으로 함께 해소. cf. [[트러블슈팅_블랙홀_260620]] |
| N2 | 🟢 해결 | 목표(은하) 포기(마무리) 후 블랙홀에 은하가 안 보이고 복구 경로도 없음(보관함이 행성만 조회) | Should/데드엔드 | — | 0620 — `loadBlackholeGalaxies`/`restoreGalaxy` 추가, BlackHolePage "보관된 목표"·"보관된 행성" 2섹션+통합 되살리기. 0618 평가서 한계(목표 단위 블랙홀 미노출) 해소. cf. [[개발일지_260620]] |
| N1 | 🟢 해결 | 중간점검 보완으로 추가한 행성이 대시보드 Today 카드에 안 보임 | 핵심 루프/표시 | — | 0620 — Today 카드가 `steps.slice(0,3)`로 앞 3개만 렌더 → 맨 뒤 새 행성 잘림. `todayStarId` 필터 단일 소스로 목록·카운트 통일, slice 제거(오늘 별 전부 표시). cf. [[개발일지_260620]] |
| M2 | 🟢 해결 | 별 개수가 디데이 당일을 제외(시작 포함·끝 제외 비대칭) → 목표일 당일 체크할 별 없음 + 당일 목표 생성 불가 | 핵심 모델/온보딩 | — | 0619 — 별 개수 `starCount`(오늘~디데이 양끝 포함)로 분리, `daysUntil`은 D-N 카운트다운 전용(당일=D-DAY). 생성 범위 `[오늘…디데이]`. cf. [[개발일지_260619]] |
| M1 | 🟢 해결 | 신규 가입 시 닉네임 화면(`/welcome`) 건너뛰고 온보딩으로 빠짐(profileReady 조회 전 afterAuth가 이동하는 레이스) | 가입 온보딩 | — | 0619 — AuthPage가 닉네임 조회 후 분기(없으면 /welcome), AuthStore 조회 `.catch` 안전장치. 이메일·카카오 공통. PR #48 |
| L13 | 🟡 배포대기 | 공유 이미지 `og_image.png` 2MB(과대) | 전송량 | — | 0618 후속 — sharp 1200×825·195KB(−90%)로 압축, og:image 치수 갱신. PR #38 머지·배포 시 반영 |
| L12 | 🟡 배포대기 | **GitHub Pages 배포 실패 → 라이브에 OG 미반영(og_image 404)** | 배포/공유 | — | 0618 후속 — vitest 도입 후 lock의 esbuild optional 플랫폼 패키지 문제로 CI `npm ci` 실패(#37 EUSAGE → #38 EBADPLATFORM). 락 재생성만으론 부족(로컬 npm11↔CI npm10 skew) → **deploy.yml을 `npm install`로 전환**해 해결. PR #38(락·og), 후속 PR(워크플로) |
| L11 | 🔴 미착수 | `self-insight`도 mid-check처럼 목표 맥락 밀착 손질 후보(현재 일반 인사이트) | LLM 품질 | — | 0618 후속 — mid-check(L4)와 동일 원칙 적용 검토 |
| L10 | 🔴 미착수 | 별자리 3D 윤곽이 **생성 후에만** 표시 — 미달성(14↑) 미리보기·3D 중심 상징 미적용 | 별자리 표현 | — | 0618 후속 — 조건 `starsEarned>=14` 확장·중심 이모지 옵션 |
| L9 | 🔴 미착수 | 3D 행성 자유 배치 시 행성 수 적으면 드물게 근접/겹침(겹침 방지 미적용) | 3D 시각 | — | 0618 후속 — 최소 간격 로직 후보 |
| L8 | 🟢 해결 | 오늘 투두가 평면 나열 + 각 행에 은하명 중복 | 가독성 | — | 0618 후속 — 목표별 양피지 드롭다운(기본 접힘) + 행성명 옆 체크/총 별, 은하명 제거(TodayPage·CheckRow) |
| L7 | 🟢 해결 | 대시보드 완성 별자리를 크게 볼 수 없음 | 보상 열람 | — | 0618 후속 — `ConstellationViewModal`(뱃지 클릭 → 모달) |
| L6 | 🟢 해결 | 별자리가 체크된 별·3D와 무관 + 그림이 "두세줄"(윤곽 정렬 부재, AI는 상징만 제공) | 기능 정확도/시각 | — | 0618 후속 — 무게중심 각도순 닫힌 윤곽: 2D 뱃지 단일화 + 3D 체크된 별 `<lineLoop>` 연결(`constellationLoop`) |
| L5 | 🟢 해결 | mid-check LLM 메시지가 목표 맥락 미반영(일반론, 예 "잘 놀기"에도 부담 줄이기) | LLM 품질 | — | 0618 후속 — 프롬프트 목표성격 우선·실제 별/한줄평 근거·상투어 회피(보편 원칙) + `PROMPT_VERSION` 캐시 무효화, `mid-check` 재배포 |
| L4 | 🟢 해결 | MidCheck 로딩 카드가 진행바(ClarityBar) 가림 + 로딩 중 "목표 진행하기" 버튼 노출 | 표시 정합 | — | 0618 후속 — 로딩 카드 `margin-top`, 액션 블록 전체 `!loading` 가드 |
| L3 | 🟢 해결 | 점검 탭에서 전체↔목표별 점검 전환 수단 부재 | 접근성 | — | 0618 후속 — 제목 옆 햄버거 → git-tree(세로선+점) 메뉴 `CheckGoalMenu`, `/app/check[/:id]` 전환 |
| L2 | 🟢 해결 | 우주 탭이 정적 2D scatter(PNG) — "나의 우주" 공간감 부족 | 컨셉/시각 | — | 0618 후속 — Three.js(R3F) 실시간 3D로 전환(은하=목표/큰 점=행성/작은 점=별, 회전·줌·팬·탭 진입), lazy 코드 스플릿 |
| L1 | 🟢 해결 | 전 페이지 상단 뒤로가기가 목적지 하드코딩(`to="/app"` 등) | 내비게이션 | — | 0618 후속 — `navigate(-1)`(이전 페이지) + 라벨 없는 화살표 아이콘(BackRow, 온보딩 제외) |
| K3 | 🟢 해결 | `AuthPage.afterAuth`가 로그인 직후 `goals` 로드 전 `length`로 분기 → 기존 유저가 `/app` 대신 `/mode`로 갈 수 있음(사전존재) | 진입 라우팅 | — | 0618 리뷰 발견. **0621 해결** — 닉네임 있으면 `goals` 로드 무관 `/app` 직행으로 분기 단순화(레이스 제거). cf. O6 · [[개발일지_260621]] |
| K2 | 🟢 해결 | 대시보드 Mid-Check 배지(라이브 상태)와 메시지("점검 전")가 모순 | 표시 정합 | — | 0618 — 저장된 점검값 기준 통일(없으면 둘 다 "점검 전") |
| K1 | 🟢 해결 | 가입 후 닉네임을 직접 정할 수 없음(트리거 자동 채움) | 온보딩 | — | 0618 — 트리거 null화 + `/welcome` 닉네임 입력 + 가드. 앱이 닉네임 호명(인사·self-insight) |
| J3 | 🟢 해결 | 별자리 생성 조건이 배정된 총 별(`goal.stars`) 기준 — 기획은 누적 체크 별 기준 | 기능 정확도 | — | 0618 — `starsEarned >= 14`로 정정(Dashboard·Universe) |
| J2 | 🟢 해결 | 별자리(constellation) 기능 미구현 — 14개↑ 보상 부재 | Should | — | 0618 — 상징 추천(LLM) + 절차적 그리기(ConstellationArt) + 모달 생성, 은하당 1개 |
| J1 | 🟢 해결 | 블랙홀 보관 행성 조회·복원 불가(`/app/blackhole` ComingSoon) | Should | — | 0618 — BlackHolePage(선택 복원), loadBlackholePlanets·restorePlanets |
| I4 | 🟢 해결 | 하단 "점검" 탭이 대표 목표로 리다이렉트만 함(우주 전체 관점 부재) | 기능 | — | 0617 — `UniverseCheckPage`: 전체 목표+한줄평 → 자기 인사이트(상태·키워드·자주 하는 말·싫어하는 것). `self-insight` 함수 배포 |
| I3 | 🟢 해결 | 중간점검 SHOULD 기능 미구현(`/app/check` ComingSoon) | 핵심 SHOULD | — | 0617 — §5-3 상태 레이블 판단 + 진행률 + 결과 저장 + 보완/마무리 액션 풀스택 구현(`feat/ai-midcheck`) |
| I2 | 🟢 해결 | 보완에서 기존 행성 전부 해제 시 빈 은하(별 0개)가 활성으로 남아 복구 불가 | 데드엔드 | — | 0617(리뷰) — 확인 버튼 가드(최소 1개 유지/추천 필요) |
| I1 | 🟢 해결 | `historyOf`가 블랙홀 행성 별까지 포함 → 점검 통계가 진행률(블랙홀 제외)과 불일치, 버린 행성이 판정 끌어내림 | 판정 정확도 | — | 0617(리뷰) — `historyOf`도 블랙홀 행성 제외 |
| H2 | 🟢 해결 | 오늘의 투두 회고를 별 체크해야만 입력 가능 + DONE/카운트 중복 | 입력 마찰·시각 | — | 0617 — `review` 컬럼·`setStarReview` 추가, 체크 무관 회고 토글 버튼(연필색=유무/둥근네모=열림). DONE 스티커 앱 전체 제거 |
| H1 | 🟢 해결 | **별 체크가 새로고침 시 원복**(DB 미반영) — 회고는 정상 저장되는데 체크만 안 됨 | 데이터 영속 | — | 0617 — `toggleStarToday`가 `starId`를 `setRows` 업데이터(나중 실행) 안에서 잡아 `if(!starId) return`에 걸려 write skip. `rows`에서 동기적으로 별을 구하도록 수정 |
| G1 | 🟡 배포대기 | 모호한 목표를 엉뚱한 도메인으로 분해(예 "책읽기"→독해훈련/단어암기/심화분석) | 분해 품질 | — | 0617 — 프롬프트 **INTERPRETATION** 규칙 추가: 목표를 자연스러운 의미·도메인대로, 여가↔학습/습관↔고강도훈련 변질 금지. 재배포 필요 |
| F1 | ~~🔴 미착수~~ 🟡 배포대기 | 무의미한 목표 입력(예 `ddd`, 반복문자) → LLM이 억지로 분해 | 분해 품질 | — | 0617 — ~~하이브리드(C): 클라 휴리스틱 + LLM 거절 가드~~ → **FALLBACK 채택**: 거절 대신 의미 없는 입력은 "목표를 정하는 과정"으로 분해(프롬프트만, UI/스키마 불변). 재배포 필요 |
| E3 | 🟢 해결 | `/plan` 재진입마다 재분해돼 편집/리프레시 결과 유실·개수 변동(예 5→4) | 입력 마찰·정합 | — | 0617 — 모듈 draft 캐시(`sig=isAuto│goal│dday│intensity`)로 입력 동일 시 복원·재요청 skip(B안). 입력 바뀌면 재분해, confirm 시 비움 |
| E2 | 🟢 해결 | `/goal`에서 뒤로가기 시 모드(알아서/추천·살살/스파르타) 선택 미보존 | 입력 마찰 | — | 0617 — GoalInputPage BackRow에 `state` 전달 + ModePage `useLocation` 초깃값 복원(`/plan→/goal` 패턴 확장). 애초에 미구현이었음 |
| E1 | ~~🟡 배포대기~~ 🟢 해결 | **goal-breakdown 부팅 실패(BOOT_ERROR)** → CORS preflight 503, 분해 전면 불가 | 분해 전면 불가 | — | 0617 — `count` 정규화 `const count`가 rate limit `const { count }`와 같은 스코프 중복 선언 → `SyntaxError`. `planetCount`로 변경. **로컬 빌드는 Edge Function 미컴파일이라 못 잡음**. ~~재배포 필요~~ → **재배포 완료, 분해 정상** |
| D3 | 🟢 해결 | 분해 리프레시 시 추가/삭제한 행성 개수가 유지 안 됨(5개→4개) | 리프레시 정합 | — | 0617 — `count`(현재 개수, 3~5 클램프)를 함수에 전달 + `inputHash` 반영. ~~재배포 완료~~ → ~~E1(BOOT_ERROR)로 실패~~ → E1 수정·재배포 완료(정상) |
| D2 | 🟢 해결 | 분해된 행성이 두루뭉실한 큰 범위라 "하루 할 일"로 와닿지 않음 | 분해 품질 | — | 0617 — 프롬프트를 첫 1~2단계 집중·구체적 하위목표로 재작성(개수별 배분 3→2:1·4→3:1·5→3:2). ~~재배포 완료~~ → ~~E1(BOOT_ERROR)로 실패~~ → E1 수정·재배포 완료(정상) |
| D1 | 🟢 해결 | 살살/스파르타 강도 경계가 모호(프롬프트 한 줄뿐) | 분해 품질 | — | 0617 — 강도별 spec 명시(easy=투두1·여유 / spartan=투두2~3+수치 목표 필수). ~~재배포 완료~~ → ~~E1(BOOT_ERROR)로 실패~~ → E1 수정·재배포 완료(정상) |
| C3 | ~~🟡 배포대기~~ 🟢 해결 | 추천 분해 리프레시 시 캐시(`input_hash`)로 같은 결과 반환 | 리프레시 무효 | — | 0617 — `exclude`를 프롬프트+`inputHash`에 포함. ~~`functions deploy goal-breakdown` 재배포 후 유효~~ → **0617 ~~재배포 완료~~ → ~~E1(BOOT_ERROR)로 실패~~ → E1 수정·재배포 완료(정상)** |
| C2 | 🟢 해결 | 입력 포커스 시 전역 `:focus-visible` 링이 안쪽 `.input`에만 둘러쳐져 래퍼 전체가 안 변함 | 폼 시각 일관성 | — | 0617 — `.input:focus/:focus-visible{outline:none}`로 내부 링 제거, `.wrap:focus-within`에 일임 |
| C1 | 🟢 해결 | D-Day 유효성 에러가 *목표 입력칸* 아래에 잘못 표시 | 에러 위치 혼란 | — | 0617 — `ddayError` 상태 분리해 디데이 칸 아래 표시(+ D-Day 필수화) |
| B6 | 🟢 해결 | D-Day 입력칸 커스텀 캘린더 SVG가 기본 date picker와 중복 | 시각적 중복 | — | 0617 — `CalendarIcon`·`right` 사용 제거 |
| B5 | 🟢 해결 | 분해 EmptyView "목표 다시 입력"이 강도·입력방식까지 초기화 | 상태손실 | — | 0617 — `navigate('/goal', { state: { mode, inputMethod } })`로 유지 |
| B4 | 🟢 해결 | **`/plan` 뒤로가기 시 목표·디데이 입력값 리셋** | 입력 마찰 | — | 0617 — `BackRow.state` prop 추가 + `GoalInputPage` 초깃값 복원으로 router state 양방향 전달 |
| B3 | 🟢 해결 | `/breakdown` 라우트명이 직접입력 흐름과 의미 불일치 | 네이밍 | — | 0617 — `/plan`·`PlanPage`로 개명(`git mv`). `lib/api.breakdownGoal`은 유지 |
| B2 | 🟢 해결 | "알아서" 모드인데 버튼이 "분해받기"로 표기 — 동작과 라벨 불일치 | UX 혼란 | — | 0617 — 라벨 분기("내 우주 직접 그리기") |
| B1 | 🟢 해결 | "알아서" 모드에 무의미한 강도(살살/스파르타) 노출 — 죽은 UI | UX 혼란 | — | 0617 — 추천 모드일 때만 조건부 노출 |
| A1 | 🟢 해결 | **천체·배경 이미지 과대 용량** (천체 ~9.5MB / 배경 3.6MB) | 모바일·GitHub Pages 초기 로딩·Lighthouse 저하 | JiwonOh | 0616 — sharp 리사이즈(PR #8, 천체 6.4MB→99.5KB) + cwebp 변환(배경 3.6MB→142KB, −96%) |

---

## A1. 천체 이미지 과대 용량 (상세)

**현재**
| 파일 | 용량 | 표시 크기 |
|---|---|---|
| `public/assets/bg/nebula.png` | 3.15MB | 배경 전면 |
| `public/assets/celestial/blackhole.png` | 2.64MB | 최대 34px |
| `public/assets/celestial/constellation.png` | 2.19MB | 66×32px |
| `public/assets/celestial/planet.png` | 1.57MB | 최대 92px |
| **합계** | **~9.5MB** | — |

**목표**: 합 300KB 이하. 파일 경로·명·.png 확장자·알파 채널 유지(코드 무수정). 표시 크기의 2배(레티나)로 리사이즈 + 압축(`sharp`/pngquant).

**사용 위치**: `src/components/ui/Celestial.jsx`(planet/blackhole/constellation), `src/components/layout/CosmicBackground.jsx`(nebula).

**경위**: PR #5(천체 이미지 교체, 머지 d116632) 이후 용량 급증 확인 → 팀원에게 최적화 PR 요청. 기획 아키텍처의 "경량 + Canvas 렌더" 지향과도 어긋나 우선 개선 대상.
