---
title: Lumiverse 평가보고서
date: 2026-06-17
phase: 2주차 MVP 개발
basis: 「프론트엔드 필수 규칙」 · Must 유저플로우 · UX 정합성
related: "[[개발일지_260617]] · [[평가보고서_260616]] · [[이슈_트래킹]]"
---

# 평가보고서 — 2026.06.17

> [!abstract] 종합
> 어제 구조적 안정화(인증 가드·FK 23503)를 마친 온보딩 흐름의 **UX 정합성 결함**을 정리한 세션. 입력방식에 따라 무의미하던 선택지를 감추고(조건부 노출), 동작과 어긋나던 라벨·라우트명을 바로잡고, 화면 전환 시 사용자 입력이 보존되도록 router state를 양방향화했다. **기능 추가보다 흐름 품질·일관성 개선**에 해당하며, 빌드·정적 검증 통과. 남은 관문은 동일하게 **사람 손 QA·Should 기능**.

---

## 1. 평가 기준

1. UX 정합성 — 사용자가 보는 선택지·라벨·전환이 실제 동작과 일치하는가
2. 상태 관리 — 화면 전환 중 입력값이 의도대로 보존/초기화되는가
3. 코드 품질 — 네이밍·하위호환·중복 제거가 적절한가

---

## 2. 항목별 평가

| 항목 | 평가 | 근거 | 점수 |
|---|---|---|---|
| **UX 정합성** | 우수 | 죽은 UI(강도) 조건부화, 거짓 라벨·라우트명 정정으로 화면=동작 일치 회복 | 9/10 |
| **상태 관리** | 우수 | 라우트 형제 간 `location.state` 양방향 계약 확립. 뒤로가기·"다시입력" 양쪽 보존 규칙 정의 | 9/10 |
| **코드 품질** | 우수 | `git mv`로 히스토리 보존, `BackRow.state` 선택적 prop으로 하위호환, 중복 SVG 제거 | 9/10 |
| **검증** | 양호 | 빌드·grep·하위호환 정적 확인. 사람 손 클릭 QA는 미수행 | 7/10 |

---

## 3. 변경의 효과

> [!success] 개선
> - **죽은 UI 제거**: "알아서" 모드에서 아무 영향 없던 강도 선택을 감춰 인지 부하·오해 제거
> - **화면 = 동작 일치**: "분해받기" 라벨/`breakdown` 라우트가 직접입력 흐름과 어긋나던 모순 해소
> - **입력 보존**: 온보딩 뒤로가기 시 목표·디데이 손실 차단 → 재입력 마찰 제거
> - **재사용 패턴**: `BackRow.state`는 다른 화면에서도 뒤로가기 시 컨텍스트 전달에 활용 가능

> [!warning] 한계
> - 사람 손 클릭 QA 미수행(빌드·정적 검증만) — 전진/후진 반복, "알아서"↔"추천" 토글 시나리오 실측 권장
> - 자동화 테스트 여전히 0 — 이런 상태 보존 회귀는 라우팅 통합 테스트로 잡기 좋은 후보
> - Should(중간점검·블랙홀·별자리) 미착수는 어제와 동일

---

## 4. 이슈 처리 현황

이번 세션 발견 6건(B1~B6) **전부 해결**. 상세는 [[이슈_트래킹]] 참조.

| 분류 | 건수 | 상태 |
|---|---|---|
| UX(죽은 UI·라벨) | 2 (B1·B2) | 🟢 |
| 네이밍(라우트) | 1 (B3) | 🟢 |
| 상태손실(뒤로가기·재입력) | 2 (B4·B5) | 🟢 |
| 정리(중복 아이콘) | 1 (B6) | 🟢 |

---

## 5. 종합

| 항목 | 점수 |
|---|---|
| UX 정합성 | 9 / 10 |
| 상태 관리 | 9 / 10 |
| 코드 품질 | 9 / 10 |
| 검증 | 7 / 10 |
| **종합** | **8.5 / 10** |

**권고**: ① 온보딩 전진/후진·모드 토글 시나리오 사람 손 QA → ② 라우팅 상태 보존 통합 테스트 후보화 → ③ Should 기능 착수.

---

# 평가보고서 (후속) — 2026.06.17 · 2차

> [!abstract] 종합
> PR #15 머지 후 온보딩 후속 작업 3건: **D-Day 필수화 · 입력 포커스 테두리 정리 · 리프레시(다른 분해 추천)**. 입력 검증·에러 위치·포커스 시각화 같은 **폼 UX 디테일**과, 캐시까지 고려한 **풀스택 기능(프론트+Edge Function)**을 더했다. 빌드 통과, ~~단 Edge Function은 **재배포 전 미적용**~~ → **0617 재배포 완료**·사람 손 QA 미수행이 남은 관문.

## 6. 후속 항목별 평가

| 항목 | 평가 | 근거 | 점수 |
|---|---|---|---|
| **폼 검증(D-Day 필수)** | 우수 | 필수화 + 에러 위치 버그(목표 칸 오표시) 동시 해소. 다운스트림 폴백 영향 점검 | 9/10 |
| **포커스 시각화** | 우수 | 전역 링이 내부 input에만 걸리던 근본 원인 추적 → `.wrap` 일임으로 공용 컴포넌트 전반 개선 | 9/10 |
| **리프레시(풀스택)** | 양호 | 랜덤 대신 "제외" 방식으로 체감 차이 보장 + 캐시 키 정합. ~~단 **Edge Function 재배포 의존**~~ → 배포 완료·deno 로컬 검증 부재 | 8/10 |

> [!success] 강점
> - 폼 디테일(필수·에러 위치·포커스)을 **공용 컴포넌트 레벨**에서 고쳐 파급 효과 큼
> - 리프레시를 캐시 키까지 고려해 설계 — 단순 reroll의 "같은 결과" 함정 회피

> [!warning] 한계 / 리스크
> - ~~**Edge Function 미배포 시 리프레시 무효**(캐시 히트로 동일 결과) — 배포가 선결~~ → **0617 재배포로 해소**
> - 로컬 `deno` 부재로 함수 타입체크 미수행(배포 시 검증)
> - 리프레시 연타 시 `RATE_LIMIT_PER_HOUR=30` 도달 가능
> - **기획 변경(디데이 자유→필수)** 문서 반영 미완
> - 사람 손 클릭 QA·자동화 테스트 여전히 미수행

## 7. 후속 종합

| 항목 | 점수 |
|---|---|
| 폼 검증 | 9 / 10 |
| 포커스 시각화 | 9 / 10 |
| 리프레시(풀스택) | 8 / 10 |
| 검증 | 7 / 10 |
| **종합** | **8.3 / 10** |

**권고**: ① ~~`goal-breakdown` 재배포~~ → 완료, 리프레시 실호출 검증(제외 반영·캐시 미스 확인) → ② 디데이 필수·과거날짜·포커스 QA → ③ 기획서/회의록에 '디데이 필수' 결정 반영.

---

# 평가보고서 (후속) — 2026.06.17 · 3차

> [!abstract] 종합
> 분해 **결과 품질** 개선: 리프레시 개수 유지(count) + goal-breakdown 프롬프트 재작성(첫 1~2단계 집중 세분화·강도 경계 명확화·스파르타 수치 목표). **별 모델은 불변, 프롬프트만** 손대 범위를 좁게 유지(다음 단계는 중간점검에 위임). ~~Edge Function 0617 재배포 완료 — 남은 건 결과물 사람 손 QA.~~ → **재배포가 BOOT_ERROR로 실패(§9 참조) — 변수 충돌 수정 후 재배포 필요.**

## 8. 후속(3차) 항목별 평가

| 항목 | 평가 | 근거 | 점수 |
|---|---|---|---|
| **개수 유지(count)** | 우수 | 3~5 클램프 + `inputHash` 반영. "5→4 줄어듦" 버그 해소 | 9/10 |
| **분해 세분화** | 양호 | 첫 1~2단계 집중·개수별 배분으로 "하루 할 일" 지향. 단 LLM 준수도는 프롬프트 의존(soft) | 8/10 |
| **강도 경계** | 양호 | easy/spartan spec 분리 + 스파르타 수치 필수 명시. 수치 포함은 코드 검증 아닌 프롬프트 유도 | 8/10 |

> [!success] 강점
> - 별 모델·스키마·검증 불변으로 **변경 표면을 프롬프트 1곳에 국한** → 리스크 작고 롤백 쉬움
> - "다음 단계는 중간점검" 위임으로 진행형 모델의 복잡도를 미루되 분해는 근시일 실행 중심으로 전환

> [!warning] 한계 / 리스크
> - 세분화·강도·수치 모두 **프롬프트 soft 지시** — 모델이 가끔 빗나갈 수 있음(검증은 3~5·필드 존재만)
> - 첫 1~2단계만 분해 → **다음 단계 전환(중간점검 시 별 제목 교체)은 미구현**(향후)
> - 결과물(살살 vs 스파르타 실차이·행성 구체성) **사람 손 QA 미수행**

**권고**: ① 살살/스파르타 동일 목표 비교로 수치·밀도 실차이 확인 → ② 분해 행성 구체성 체감 점검 → ③ (향후) 중간점검에서 다음 단계 별 제목 교체 설계.

---

# 평가보고서 (후속) — 2026.06.17 · 4차

> [!abstract] 종합
> 3차 재배포가 **BOOT_ERROR(변수명 충돌)** 로 실패해 분해가 전면 불가(CORS 503)였음을 발견·수정. 더해 `/goal` 뒤로가기 모드 보존(미구현) 보완. 핵심 교훈: **Edge Function은 Vite 빌드 밖이라 로컬 빌드로 검증되지 않는다.**

## 9. 후속(4차) 항목별 평가

| 항목 | 평가 | 근거 | 점수 |
|---|---|---|---|
| **BOOT_ERROR 진단·수정** | 우수 | OPTIONS 503·`sb-error-code: BOOT_ERROR`로 원인 특정 → `const count` 중복 선언 → `planetCount`로 해결 | 9/10 |
| **모드 state 보존** | 우수 | `/plan→/goal` 패턴을 `/goal→/mode`로 확장(`BackRow.state` 재사용) | 9/10 |
| **재진입 보존(B안)** | 양호 | 모듈 draft 캐시로 입력 동일 시 재분해 skip·편집 유지. 단 최근 sig 1개만 보관·새로고침 초기화 | 8/10 |
| **배포 검증 체계** | 미흡 | 함수 부팅 오류를 배포 후에야 인지. `npm run build`로 안 잡힘 | 5/10 |

> [!danger] 프로세스 리스크 (재발 방지 필요)
> - **Edge Function은 `npm run build`로 검증 안 됨** — "빌드 통과 ✅"가 함수 정상 배포를 보장하지 않음. 배포 전 `deno check` 또는 CI에 Deno 린트 단계 추가 권장.
> - 지난 "재배포 완료" 기록은 실제론 BOOT_ERROR 배포였음 — 문서에 취소선으로 정정.

**권고**: ① ~~변수 충돌 수정본 재배포 후 OPTIONS 200·실제 분해 동작 확인~~ → **재배포 완료·분해 정상 확인됨** → ② Edge Function 배포 전 `deno check` 루틴화 → ③ `/goal↔/mode` 전·후진 모드·재진입 보존 QA → ④ **무의미 목표 입력 필터(F1)** — 하이브리드(클라 휴리스틱 + LLM 거절 가드) 설계·구현.
