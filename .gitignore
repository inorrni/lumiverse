# obsidian (vault settings — local only, not for deploy repo)
.obsidian/

.DS_Store

# yumi 개인 문서 (로컬 전용)
yumi_docs/
CLAUDE.md

# 의존성 / 빌드 산출물
node_modules/
dist/
dist-ssr/

# 환경변수 · 비밀키 (절대 커밋 금지)
.env
.env.local
.env.*.local
*.pem

# Deno (Edge Functions) — `deno check`/`deno cache` 부산물, 루트엔 불필요
deno.lock

# 로그 · 캐시
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.cache/

# 에디터
.vscode/*
!.vscode/extensions.json
*.suo
*.ntvs*
*.njsproj
*.sln
